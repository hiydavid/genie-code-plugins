# Runtime dependencies installed by Databricks Apps.
# The SDK is used for SQL statement execution (version persistence) and for raw
# REST calls to the Genie spaces API via api_client.do (live read on save,
# etag-guarded update on restore), both as the calling user through OBO
# (`sql` and `genie` scopes, as declared in app.yaml).
anyio>=4
databricks-sdk>=0.118.0
fastapi>=0.115.12
uvicorn>=0.34.2
# fastmcp/mcp are MAJOR-version-pinned: 2.x and 3.x have incompatible APIs (e.g.
# FastMCP.http_app() routing/mounting changed between majors), so an unbounded floor
# would make installs non-deterministic across the 2.x->3.x boundary. We target 3.x
# (validated end-to-end: http_app(path=..., stateless_http=True) ->
# StarletteWithLifespan mount + @tool).
fastmcp>=3.4,<4
mcp[cli]>=1.14.0,<2
pydantic>=2
